---
'style-dictionary': minor
---

Support loading TypeScript token files (`.ts`, `.mts`, `.cts`) as sources, includes and via custom globs, using the runtime's native TypeScript support (Deno, Bun, or Node.js >=22.6 with `--experimental-strip-types`, enabled by default since v23.6).
