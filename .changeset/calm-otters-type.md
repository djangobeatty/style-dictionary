---
'style-dictionary': minor
---

Support TypeScript (`.ts`, `.mts`) design token files and config files. They are loaded through the native TypeScript support of your JavaScript runtime (Deno, Bun, or Node.js with type stripping), so no transpilation step is needed.
